# Contracts

This feature operates entirely on the client with no network-based APIs.

- No HTTP endpoints are required for core actions (upload, scroll, rarity, WOW).
- The OpenAPI file documents that there are no exposed server APIs for this feature.